---
// Value Section component
---

<section class="value-section">
  <div class="container">
    <h2 class="value-title">+20 AÑOS DE EXPERIENCIA</h2>
    <p class="value-description">
      Si bien todos los caminos llegan a Roma, no todos los caminos son iguales. 
      En MR Roma ofrecemos el valor de la experiencia para potenciar tu talento interno y llegar a Roma, sin darse tantas vueltas.
    </p>
    <div class="services-grid">
      <div class="service-item" data-service="talleres">
        <div class="service-number">01</div>
        <h3 class="service-title">TALLERES</h3>
        <p class="service-description">Capacitación práctica y experiencial para equipos de alto rendimiento</p>
        <div class="service-line"></div>
        
        <!-- Hover reveal content -->
        <div class="service-hover-content">
          <div class="hover-content-text">
            <p>Realizamos el levantamiento de los objetivos de negocios para alinear las iniciativas de Producto y Tecnología con estos y realizar la bajada en un taller dinamico que asegure la sincronizacion de tus equipos con los objetivos de Negocio.<p>
          </div>
        </div>
      </div>
      
      <div class="service-item" data-service="mentorias">
        <div class="service-number">02</div>
        <h3 class="service-title">MENTORÍAS</h3>
        <p class="service-description">Acompañamiento personalizado en el desarrollo de liderazgo</p>
        <div class="service-line"></div>
        
        <!-- Hover reveal content -->
        <div class="service-hover-content">
          <div class="hover-content-text">
            <p>Potenciamos tu talentos internos y liderazgo traspasando el conocimiento acumulado de la experiencia de haber trabajado con equipos interdisciplinarios en distintas industrias y países.</p>
          </div>
        </div>
      </div>
      
      <div class="service-item" data-service="asesorias">
        <div class="service-number">03</div>
        <h3 class="service-title">ASESORÍAS</h3>
        <p class="service-description">Consultoría estratégica especializada en transformación digital</p>
        <div class="service-line"></div>
        
        <!-- Hover reveal content -->
        <div class="service-hover-content">
          <div class="hover-content-text">
            <p>Apoyamos en la construcción de la estrategia e iniciativas de Producto y Tecnología para cumplimiento de los objetivos estratégicos del Negocio.</p>
          </div>
        </div>
      </div>
      
      <div class="service-item" data-service="lanzamientos">
        <div class="service-number">04</div>
        <h3 class="service-title">LANZAMIENTOS</h3>
        <p class="service-description">Implementación y ejecución de proyectos críticos</p>
        <div class="service-line"></div>
        
        <!-- Hover reveal content -->
        <div class="service-hover-content">
          <div class="hover-content-text">
            <p>Apoyamos en la construcción de estrategia de lanzamientos de nuevos productos y servicios digitales para asegurar un impacto en el ecosistema en el que compite.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .value-section {
    padding: 5rem 0; /* Further reduced from 6rem */
    background-color: transparent;
    position: relative;
  }

  .value-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: transparent;
    pointer-events: none;
  }

  .container {
    max-width: 80%;
    margin: 0 auto;
    padding: 0 2rem;
  }

  @media (min-width: 1400px) {
    .container {
      max-width: 70%;
    }
  }

  .value-title {
    font-size: clamp(2.5rem, 4vw, 3.5rem);
    font-weight: 900;
    text-align: center;
    margin-bottom: 1.5rem;
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
    position: relative;
    z-index: 1;
    letter-spacing: -0.08em;
    line-height: 1.1;
    text-transform: uppercase;
    color: #000000;
  }

  .value-description {
    text-align: center;
    max-width: 700px;
    margin: 0 auto 3rem;
    font-size: clamp(1rem, 2vw, 1.3rem);
    font-weight: 300;
    line-height: 1.4;
    position: relative;
    z-index: 1;
    color: #000000;
    letter-spacing: 0.02em;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem; /* Further reduced from 2rem */
    max-width: 100%;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  @media (max-width: 767px) {
    .services-grid {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
  }

  .service-item {
    text-align: left;
    padding: 1.5rem 1.25rem; /* Further reduced from 2rem 1.5rem */
    border: 2px solid #000000;
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    cursor: pointer;
    position: relative;
    overflow: hidden;
    background: #ffffff;
    min-height: 160px; /* Further reduced from 200px */
    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }

  .service-item::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: #f26168;
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    z-index: -1;
  }

  .service-item:hover::before {
    transform: scaleX(1);
  }

  .service-item:hover {
    color: #ffffff;
    transform: translateY(-10px);
    box-shadow: 0 25px 50px rgba(0,0,0,0.2);
  }

  .service-number {
    font-size: 2.5rem; /* Further reduced from 3rem */
    font-weight: 100;
    color: #000000;
    margin-bottom: 0.5rem; /* Further reduced from 0.75rem */
    transition: all 0.3s ease;
    line-height: 1;
    opacity: 0.3;
  }

  .service-item:hover .service-number {
    color: #ffffff;
    opacity: 0.7;
  }

  .service-title {
    font-size: clamp(1rem, 2.5vw, 1.5rem); /* Responsive sizing */
    font-weight: 900;
    letter-spacing: 0.15em;
    margin-bottom: 0.75rem; /* Further reduced from 1rem */
    transition: color 0.3s ease;
    line-height: 1.2;
  }

  .service-description {
    font-size: clamp(0.75rem, 2vw, 1.2rem); /* Responsive sizing */
    font-weight: 300;
    line-height: 1.6;
    transition: color 0.3s ease;
    flex-grow: 1;
    margin-bottom: 1rem; /* Further reduced from 1.25rem */
  }

  .service-line {
    width: 40px; /* Further reduced from 50px */
    height: 2px;
    background-color: #000000;
    transition: all 0.3s ease;
  }

  .service-item:hover .service-line {
    background-color: #ffffff;
    width: 60px; /* Further reduced from 80px */
  }

  .service-item:hover .service-title,
  .service-item:hover .service-description {
    color: #ffffff;
  }

  /* Hover reveal effect for new text */
  .service-hover-content {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: #f26168;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 1.25rem; /* Further reduced from 1.5rem */
    opacity: 0;
    transform: translateY(100%);
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    z-index: 5;
  }

  .service-item:hover .service-hover-content {
    opacity: 1;
    transform: translateY(0);
  }

  .hover-content-text {
    color: #ffffff;
    text-align: center;
    line-height: 1.6;
    font-weight: 400;
  }

  .hover-content-text p {
    margin-bottom: 0.5rem; /* Further reduced from 0.75rem */
    font-size: clamp(0.75rem, 2vw, 1.2rem); /* Responsive sizing */
  }

  .hover-content-text p:last-child {
    margin-bottom: 0;
  }

  /* Hide original content on hover */
  .service-item:hover .service-number,
  .service-item:hover .service-title,
  .service-item:hover .service-description,
  .service-item:hover .service-line {
    opacity: 0;
    transform: translateY(-20px);
  }

  /* Responsive */
  @media (min-width: 1400px) {
    .value-title {
      font-size: 3.5rem; /* Align with FrameworkSection */
      margin-bottom: 2rem; /* Further reduced from 2.5rem */
    }
    
    .value-description {
      font-size: 1.3rem; /* Align with FrameworkSection */
      max-width: 700px; /* Align with FrameworkSection */
    }
  }

  @media (max-width: 1399px) and (min-width: 1024px) {
    .value-title {
      font-size: clamp(2rem, 3vw, 3rem); /* Align with FrameworkSection */
      margin-bottom: 1.5rem;
    }
    
    .value-description {
      font-size: clamp(0.9rem, 1.8vw, 1.1rem); /* Align with FrameworkSection */
      max-width: 600px; /* Align with FrameworkSection */
    }
  }

  @media (max-width: 1023px) and (min-width: 768px) {
    .value-title {
      font-size: clamp(1.75rem, 3.5vw, 2.5rem); /* Align with FrameworkSection */
      margin-bottom: 1rem; /* Align with FrameworkSection */
    }
    
    .value-description {
      font-size: clamp(0.8rem, 2vw, 1rem); /* Align with FrameworkSection */
      max-width: 500px; /* Align with FrameworkSection */
    }
  }

  @media (max-width: 767px) and (min-width: 481px) {
    .value-title {
      font-size: clamp(1.5rem, 4vw, 2rem); /* Align with FrameworkSection */
      margin-bottom: 1rem; /* Align with FrameworkSection */
    }
    
    .value-description {
      font-size: clamp(0.7rem, 2.5vw, 0.9rem); /* Align with FrameworkSection */
      max-width: 450px; /* Align with FrameworkSection */
    }
  }

  @media (max-width: 480px) {
    .value-section {
      padding: 3rem 0; /* Align with FrameworkSection */
    }
    
    .value-title {
      font-size: clamp(1.25rem, 4.5vw, 1.75rem); /* Align with FrameworkSection */
      margin-bottom: 0.75rem; /* Align with FrameworkSection */
    }
    
    .value-description {
      font-size: clamp(0.6rem, 3vw, 0.8rem); /* Align with FrameworkSection */
      max-width: 100%;
      margin-bottom: 1.5rem; /* Align with FrameworkSection */
    }
    
    .services-grid {
      gap: 1rem; /* Further reduced from 1.25rem */
    }
    
    .service-item {
      padding: 1.25rem 1rem; /* Further reduced from 1.5rem 1.25rem */
      min-height: 140px; /* Further reduced from 180px */
    }
    
    .service-number {
      font-size: 1.75rem; /* Further reduced from 2rem */
    }
    
    .service-title {
      font-size: 0.875rem; /* Further reduced from 1rem */
    }
  }
</style>

<script>
  // Add interactive effects to service items
  document.addEventListener('DOMContentLoaded', function() {
    const serviceItems = document.querySelectorAll('.service-item');
    
    serviceItems.forEach(item => {
      item.addEventListener('mouseenter', function(event: Event) {
        const target = event.currentTarget as HTMLElement;
        target.style.transform = 'translateY(-10px)';
      });
      
      item.addEventListener('mouseleave', function(event: Event) {
        const target = event.currentTarget as HTMLElement;
        target.style.transform = 'translateY(0)';
      });
      
      // Add click effect
      item.addEventListener('click', function(event: Event) {
        const target = event.currentTarget as HTMLElement;
        const service = target.dataset.service;
        console.log(`Servicio seleccionado: ${service}`);
        // Aquí puedes agregar lógica adicional para cada servicio
      });
    });
  });
</script>
